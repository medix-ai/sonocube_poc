# SonoCube Landing Page — 콘텐츠 기획서

> 개발자 전달용 문서입니다. 각 섹션의 텍스트, 구조, 주의사항을 담았습니다.

---

## 메타 정보

| 항목 | 내용 |
|---|---|
| 앱 이름 | SonoCube |
| 버전 | v1.3.0 |
| 플랫폼 | macOS (Apple Silicon / Intel) |
| 카테고리 | 심장초음파 연구 도구 |
| 대상 사용자 | 심장내과 연구자, 임상 연구원, 의공학 연구팀 |

---

## 1. Hero 섹션

### 헤드라인
```
AI-Powered Cardiac EF Analysis
심장초음파 영상에서 박출계수를 자동으로 추정합니다
```

### 서브 헤드라인
```
AVI · DICOM 파일을 드래그하면
ED/ES 프레임 자동 검출 → AI EF 추론 → PDF 리포트까지
연구자의 반복 작업을 줄여줍니다
```

### CTA 버튼
- **Primary**: `macOS용 다운로드 (v1.3.0)` → `.dmg` 파일 링크
- **Secondary**: `GitHub에서 보기` → GitHub 레포 링크

### 비주얼
- 앱 스크린샷 (다크 테마 워크스테이션 UI)
- 추천 구도: 왼쪽 텍스트 / 오른쪽 앱 화면 (MacBook 목업 안에 삽입)

### 주의 배너 (Hero 하단에 작은 텍스트로 반드시 포함)
```
⚠ 이 소프트웨어는 연구 목적으로만 사용되며 진단 목적으로 사용할 수 없습니다.
Research use only. Not intended for clinical diagnosis.
```

---

## 2. Problem 섹션

### 타이틀
```
기존 심초음파 EF 분석의 한계
```

### 내용 (카드 3개 또는 리스트)

**① 판독자 의존성**
```
EF는 Simpson's biplane 방법으로 수동 측정합니다.
측정자의 숙련도와 집중도에 따라 결과가 달라집니다.
동일 환자, 동일 영상이어도 판독자 간 편차(inter-observer variability)가 존재합니다.
```

**② 반복 작업의 시간 비용**
```
연구 프로젝트에서 수십~수백 건의 echo 영상을 분석할 때
각 케이스마다 ED/ES 프레임을 직접 찾고, 경계를 그리고, 계산해야 합니다.
반복적인 수동 분석은 연구 속도를 제한하는 병목이 됩니다.
```

**③ 데이터 기록과 재현성**
```
분석 결과가 개인 파일이나 메모 형태로 남으면
추후 재현 검증이 어렵고, 연구 협력 시 데이터 공유가 번거롭습니다.
```

---

## 3. Solution 섹션

### 타이틀
```
SonoCube가 하는 일
```

### 서브타이틀
```
복잡한 설치 없이, 영상 파일 하나로 시작합니다
```

### 플로우 다이어그램 (3단계)

```
[영상 드래그 앤 드롭]  →  [ED/ES 자동 검출 + AI 추론]  →  [PDF 리포트 저장]
 AVI · DICOM 지원        ONNX 경량 모델 (MAE 8.76%)      통계 + 시각화 포함
```

### 상세 설명

**Step 1 — 파일 열기**
```
AVI, MP4, DICOM (.dcm) 파일을 앱에 드래그하거나
파일 선택 버튼으로 불러옵니다.
영상 뷰어에서 프레임을 미리 확인할 수 있습니다.
```

**Step 2 — AI 분석 실행**
```
Space 키 또는 버튼 한 번으로 분석이 시작됩니다.
영상에서 ED(이완기 말) · ES(수축기 말) 프레임을 자동으로 검출합니다.
두 프레임을 쌍으로 입력받는 ONNX 경량 모델이 EF를 추론합니다.
분석은 수초 내에 완료됩니다.
```

**Step 3 — 결과 확인 및 리포트**
```
EF 수치, 임상 참고 범위 시각화, 예측 안정성 지표를 즉시 확인합니다.
PDF 리포트를 생성하면 EF 범위 바, 프레임 통계, 품질 경고가 포함됩니다.
케이스 히스토리는 앱 내에 자동 저장되어 이력 관리가 가능합니다.
```

---

## 4. Key Features 섹션

### 타이틀
```
주요 기능
```

### 카드 구성 (6개 카드, 2열 3행 또는 3열 2행)

---

**카드 1 — ONNX 경량 모델**
- 아이콘: CPU/칩 아이콘
- 제목: `인터넷 없이 로컬 실행`
- 설명: `외부 서버에 데이터를 전송하지 않습니다. 모든 추론은 로컬 CPU에서 실행되는 ONNX 경량 모델(~60KB)로 처리됩니다. ED/ES 쌍 입력 방식으로 EchoNet 테스트셋 기준 MAE 8.76%, r=0.534를 달성했습니다. 환자 영상 데이터가 기기 밖으로 나가지 않습니다.`

---

**카드 2 — 임상 참고 범위 시각화**
- 아이콘: 심장/차트 아이콘
- 제목: `EF 범위를 직관적으로 확인`
- 설명: `결과 화면과 PDF 리포트 모두에서 EF 수치가 임상 참고 범위 위에 표시됩니다. Reduced (< 40%) / Mildly Reduced (40–54%) / Normal (≥ 55%) 구역을 색상으로 구분합니다.`

---

**카드 3 — 예측 안정성 지표**
- 아이콘: 그래프/안정성 아이콘
- 제목: `결과와 함께 품질 맥락을 제공합니다`
- 설명: `AI가 검출한 ED·ES 프레임 위치와 EF 추론값을 함께 표시합니다. 영상 품질·프레임 수·검출 신뢰도 기반의 품질 경고(High / Moderate / Info)가 제공되며, 문제 발생 시 수동 검토를 권고합니다.`

---

**카드 4 — 임상 수준 PDF 리포트**
- 아이콘: 문서 아이콘
- 제목: `케이스마다 기록을 남깁니다`
- 설명: `분석 완료 즉시 PDF 리포트를 생성할 수 있습니다. EF 범위 바, 프레임 통계 테이블, 품질 경고, 분석 메타데이터가 포함된 라이트 테마 문서입니다.`

---

**카드 5 — 이미지 품질 경고**
- 아이콘: 경고/체크 아이콘
- 제목: `낮은 품질 영상을 자동으로 감지`
- 설명: `입력 영상의 프레임 수, 밝기 분포, EF 변동성을 분석해 품질 경고를 HIGH / MODERATE / INFO 심각도로 분류합니다. 각 경고에는 조치 방향이 함께 제시됩니다.`

---

**카드 6 — 케이스 히스토리**
- 아이콘: 목록/폴더 아이콘
- 제목: `분석 이력 자동 저장`
- 설명: `모든 분석 결과는 자동으로 히스토리에 저장됩니다. 날짜, EF, 파일명으로 검색하고, 이전 케이스의 PDF를 바로 열거나 CSV로 내보낼 수 있습니다.`

---

## 5. 개발 배경 섹션

### 타이틀
```
왜 SonoCube를 만들었나
```

### 본문
```
SonoCube는 임상 연구 현장에서 반복적인 echo 분석 작업을 줄이기 위해
연구팀이 직접 설계하고 개발한 연구용 소프트웨어입니다.

상용 echo 워크스테이션은 병원 PACS 시스템에 종속되어 있어
외부 연구 환경이나 멀티센터 연구에서 동일한 분석 파이프라인을 유지하기 어렵습니다.
SonoCube는 표준 영상 파일(AVI/DICOM)만 있으면
어떤 환경에서도 동일한 AI 파이프라인으로 분석 결과를 재현할 수 있도록 설계되었습니다.

v1.3에서는 LV 분할 모델(LVSegEngine)이 통합되어
수동 프레임 선택 없이 ED/ES를 자동 검출합니다.
분할 마스크 기반의 Simpson EF 근사값도 참고용으로 함께 제공됩니다.
```

### 기술 스택 (개발자용 참고)
```
- 언어: Python 3.11
- GUI: PyQt5
- AI 추론: ONNX Runtime (CPU, Apple CoreML 가속 지원)
  - LVSegEngine: LV 분할 U-Net (1.8MB), Dice=0.930·IoU=0.871 (EchoNet TEST)
    → 프레임별 LV 마스크 → ED=최대 면적 / ES=최소 면적(시간순 제약)
  - SonoCubeV2: ED/ES 쌍 입력 경량 CNN (60KB, 6×96×96)
    → EchoNet TEST MAE 8.76%·r=0.534
  - 학습 데이터: EchoNet-Dynamic (10,030 케이스)
- 영상 처리: OpenCV
- 리포트: ReportLab
- 패키징: PyInstaller (.app / .exe)
```

---

## 6. 로드맵 섹션 (선택)

### 타이틀
```
앞으로의 계획
```

| 버전 | 목표 |
|---|---|
| v1.2 | EF 자동 추정, PDF 리포트, 케이스 히스토리 |
| v1.3 (현재) | LVSeg 통합 (Dice=0.930) + SonoCubeV2 (60KB, MAE 8.76%) — LV 마스크 기반 ED/ES 자동 검출, Simpson EF 근사 |
| v1.5 (예정) | EDV/ESV 정량 지원, 멀티케이스 배치 분석 |
| v2.0 (예정) | EF 추이 시각화, DICOM SR 내보내기, 멀티센터 연구 지원 |

---

## 7. Disclaimer 섹션 (필수 — 독립 섹션으로 반드시 노출)

### 배경색: 연한 노란색 또는 연한 회색 경고 박스

```
⚠ 연구 목적 전용 소프트웨어

이 소프트웨어는 학술 및 연구 목적으로만 개발되었습니다.
임상 진단, 치료 결정, 또는 의료 행위의 근거로 사용해서는 안 됩니다.

표시되는 EF 수치는 AI 모델의 추론 결과이며,
공인된 의료기기로 측정된 수치와 동일하지 않습니다.

Prediction stability(예측 안정성)는 프레임별 추론의 일관성을 나타내는 지표로,
임상적 신뢰도(clinical confidence)와 다른 개념입니다.

This software is intended for research use only.
It is not a medical device and must not be used for clinical diagnosis.
```

---

## 8. Download 섹션

### 타이틀
```
다운로드
```

### 버튼

| 버튼 | 링크 | 비고 |
|---|---|---|
| `macOS 다운로드 (v1.3.0)` | `.dmg` 파일 직링크 | Apple Silicon + Intel Universal |
| `GitHub 소스코드` | GitHub 레포 URL | 연구자 공개용 |

### 설치 안내 (macOS Gatekeeper 관련)
```
macOS에서 처음 실행 시 보안 경고가 표시될 수 있습니다.
시스템 환경설정 → 개인 정보 보호 및 보안에서 허용하거나
터미널에서 아래 명령어를 실행하세요:

xattr -rd com.apple.quarantine SonoCube.app
```

### 시스템 요구사항
```
- macOS 12 Monterey 이상
- Apple Silicon (M1/M2/M3) 또는 Intel Mac
- 저장 공간: 500MB 이상
- RAM: 4GB 이상 권장
```

---

## 9. Footer

```
SonoCube v1.3.0
Research use only. Not for clinical diagnosis.

© 2026 SonoCube Research Team
문의: [이메일 주소]
GitHub: [레포 URL]
```

---

## 디자인 가이드라인 (개발자 참고)

### 컬러
| 용도 | 색상 |
|---|---|
| EF 정상 (≥ 55%) | `#52c27a` (그린) |
| EF 경계 (40–54%) | `#e8a217` (앰버) |
| EF 감소 (< 40%) | `#e05252` (레드) |
| 앱 어두운 배경 | `#1a1a1a` |
| 포인트 컬러 | `#00b4cc` (시안) |
| Disclaimer 박스 | `#fff8e1` 배경 + `#e65100` 텍스트 |

### 톤
- 의료 연구 소프트웨어답게 **절제되고 신뢰감 있는** 톤
- 과장된 마케팅 표현 지양 ("혁신적", "완벽한" 등 사용 금지)
- Disclaimer는 눈에 잘 띄게, 숨기지 말 것

### 스크린샷 준비 사항
- 앱 실행 후 Study 탭에서 분석 완료 상태 캡처
- EF 수치, 범위 바, Stability 표시 모두 보이도록
- PDF 리포트 1페이지 썸네일 포함 권장
